<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css"> <!-- Include style.css -->
</head>
<body>
    <h1><%= title %></h1>
    
    <p>Status: <%= task.completed ? 'Completed' : 'Pending' %></p>

    <form action="/<%= task.id %>" method="post">
        <button class="btn btn-toggle-completion" type="submit">Toggle Completion</button>
    </form>

    <form action="/<%= task.id %>" method="post" style="margin-top: 1em;">
        <input type="hidden" name="_method" value="delete">
        <button class="btn btn-delete" type="submit">Delete Task</button>
    </form>
</body>
</html>
